<Page
    x:Class="UITEST.View.StartupQuestions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITEST.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="White"/>
        </Grid.Background>
        <RelativePanel>

            <Image  Name="pic" Source="/Assets/Welcome.png"/>
            <Button Click="nextpage_Click" x:Name="nextpage" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.Below="pic" Background="LightGray" Margin="0, -150, 0, 0" Foreground="#603BBF" Height="36" Width ="150" Content="Let's go!"  />
        </RelativePanel>
    </Grid>
    <Page.Resources>
        <Storyboard x:Key="FadeIn">

            <DoubleAnimation From="0" To="1" Storyboard.TargetName="pic" Storyboard.TargetProperty="(UIElement.Opacity)" Duration="0:0:3"/>

            <DoubleAnimation From="0" To="1" Storyboard.TargetName="nextpage" Storyboard.TargetProperty="(UIElement.Opacity)" Duration="0:0:3"/>
        </Storyboard>
    </Page.Resources>
</Page>
