<Page
    x:Class="UITEST.View.PostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITEST.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" DataContext="{x:Bind Path = _vm}">
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <CommandBar Name="commandBar" Style="{StaticResource StandardCommandBar}">
            <AppBarButton Icon="Home" Label="Home" Command="{Binding GoToHomePageCommand}"/>
            <AppBarButton Icon="ZeroBars" Label="Trending" Command="{Binding GoToTrendingPageCommand}"/>
            <AppBarButton Icon="OutlineStar" Label="Discover" Command="{Binding GoToDiscoverPageCommand}"/>
            <AppBarButton Icon="Contact" Label="Profile" Command="{Binding GoToProfilePageCommand}"/>
        </CommandBar>
        <RelativePanel Name="TopInfo"  Margin="20, 20, 0, 0" Height="Auto" Grid.Row="1" DataContext="{x:Bind Path = _vm.CurrentPost}">
            <RelativePanel  Name="OpOgNed" Width="100">
                <Button Content="Like" Height="20" Width="20" Name="op" Style="{StaticResource VoteButton}" Click="PostVoteButton_Clicked"/>
                <TextBlock Text="{Binding NumOfVotes}" Name="Votes" Width="50" RelativePanel.Below="op" RelativePanel.AlignHorizontalCenterWith="op"/>
                <Button Content="Dislike" Height="20" FontSize="30" Name="ned" RelativePanel.Below="Votes" Style="{StaticResource VoteButton}" Click="PostVoteButton_Clicked"/>
                <Rectangle Fill="Blue" Height="40" Width="40" Name="Box" Margin="20, 0, 0, 0" RelativePanel.RightOf="op"/>
            </RelativePanel>

            <RelativePanel Name="TitlePanel" RelativePanel.RightOf="OpOgNed">
                <TextBlock Text="{Binding Title}" FontSize="20" Name="Title"/>
            </RelativePanel>

            <RelativePanel Name="TextPanel" RelativePanel.Below="TitlePanel" RelativePanel.RightOf="OpOgNed" Width="Auto" Margin="0, 4, 10, 0">
                <Border BorderThickness="1" BorderBrush="Gray" Padding="10, 10, 10, 10">
                    <TextBlock Text="{Binding Text}" Name="PostText" TextWrapping="Wrap"/>
                </Border>
            </RelativePanel>

            <RelativePanel Name="ExtraStuff" RelativePanel.RightOf="OpOgNed" RelativePanel.Below="TextPanel">
                <TextBlock Text="{Binding Author}" Style="{StaticResource AuthorText}" Name="Author"/>
                <TextBlock Text="{Binding Comments.Count}" RelativePanel.Below ="Author" FontSize="10" Name="CommentsAmount"/>
                <Button Click="CommentText_Click" Content="comment" Style="{StaticResource MiniNavigationButton}" RelativePanel.RightOf="CommentsAmount" RelativePanel.Below="Author" Name="CommentButton" PointerEntered="TextButton_PointerEntered" PointerExited="TextButton_PointerLeaved"/>
                <Button Content="share" Style="{StaticResource MiniNavigationButton}" RelativePanel.RightOf="CommentButton" RelativePanel.Below="Author" Name="ShareText" PointerEntered="TextButton_PointerEntered" PointerExited="TextButton_PointerLeaved"/>
                <Button Content="save" Style="{StaticResource MiniNavigationButton}" RelativePanel.RightOf="ShareText" RelativePanel.Below="Author" Name="SaveText" PointerEntered="TextButton_PointerEntered" PointerExited="TextButton_PointerLeaved"/>
                <Button Content="hide" Style="{StaticResource MiniNavigationButton}" RelativePanel.RightOf="SaveText" RelativePanel.Below="Author" Name="HideText" PointerEntered="TextButton_PointerEntered" PointerExited="TextButton_PointerLeaved"/>
                <Button Content="report" Style="{StaticResource MiniNavigationButton}" RelativePanel.RightOf="HideText" RelativePanel.Below="Author" Name="ReportText" PointerEntered="TextButton_PointerEntered" PointerExited="TextButton_PointerLeaved"/>
            </RelativePanel>
        </RelativePanel>

        <Rectangle Grid.Row="2" Name="verticalSplitter" HorizontalAlignment="Stretch" Fill="Gray" Height="1" Margin="0, 5, 0, 10"/>

        <RelativePanel Grid.Row="3" Margin="10,0, 0, 10" Visibility="Collapsed" Name="CommentField">
            <TextBox Name="CommentTextBox" Height="200" Width="600" AcceptsReturn="True" TextWrapping="Wrap" IsSpellCheckEnabled="True" Language="en-US"/>
            <Button Content="Save" Click="CommentSaveClick"  RelativePanel.Below="CommentTextBox" Margin="0, 10, 10, 0"/>
        </RelativePanel>

        <ListView Grid.Row="4" Name="CommentsView" SelectionMode="None" Height="200" HorizontalAlignment="Left">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
